<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" media="screen">
    <style type="text/css">
      html {
        height: 95%;
      }
      body {
        height: 95%;
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
      #wrap {
        min-height: 95%;
        height: auto !important;
        height: 95%;
        margin: 0 auto -60px;
      }
      #push,
      #footer {
        height: 60px;
      }
    </style>
    <link href="/bootstrap/css/bootstrap-responsive.min.css" rel="stylesheet">
    <link href="/stylesheets/bootstrap-toggle-buttons.css" rel="stylesheet">
    <link href="/stylesheets/style.css" rel="stylesheet">
    <script src="/javascripts/jquery-1.8.2.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <script src="/javascripts/jquery.toggle.buttons.js"></script>
    <script src="/javascripts/clippy-jquery/jquery.clippy.js"></script>
  </head>

  <body>
  <div id="wrap">

    <% include _navbar %>

    <div class="container">
      <%- body %>
    </div>
    <div id="push"></div>
  </div>
  <div id="footer">
    <div class="container">
      <p class="muted credit">
        Githood copyright
        <%
          var authors = [ 'dsingley', 'EvanOxfeld', 'kunklejr'];
          for(var i = authors.length - 1; i > 0; i--) {
            var j = Math.floor(Math.random() * (i + 1));
            var tmp = authors[i];
            authors[i] = authors[j];
            authors[j] = tmp;
          }

          authors.forEach(function(a) {
        %>
          <a href="https://twitter.com/<%= a %>">@<%= a %></a>
        <%  }); %>
      </p>
    </div>
  </div>

  </body>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/javascripts/sockets.js"></script>
</html>
